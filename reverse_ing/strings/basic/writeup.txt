Challenge (https://trojanc.tf/challenges#Strings-9)
- All these strings, everywhere! Someone ought to clean them up. There should be a flag somewhere in there though.
- strings file

Solution:

$ strings strings | grep Trojan*
Trojan{Y04_F0unD_M3!}